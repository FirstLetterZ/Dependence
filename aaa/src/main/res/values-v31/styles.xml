<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Splash" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- 设置启动页窗口填充内容（drawable），此drawable将会放置在windowBackground之上，且会缩放至保证边界在可视窗口范围内（受系统栏影响）；
             如果不希望显示系统栏，请使用全屏主题。
             请注意，即使主题上没有设置windowSplashscreenContent，系统仍会使用其他属性配置启动页画面，
             如windowBackground、windowSplashscreenAnimatedIcon。 -->
        <item name="android:windowSplashscreenContent" format="reference"></item>
        <!-- 设置启动页背景颜色，如果不指定，则系统将使用windowBackground。 -->
        <item name="android:windowSplashScreenBackground" format="color">#000</item>
        <!-- 设置启动页图标，图标在页面中心显示，如果资源为可执行动画的drawable，（如AnimationDrawable, AnimatedVectorDrawable）
             则在展示启动页的同时会执行动画。 -->
        <item name="android:windowSplashScreenAnimatedIcon" format="reference"></item>
        <!-- 设置启动页图标动画时间，单位毫秒，最大时长不超过1000ms -->
        <item name="android:windowSplashScreenAnimationDuration" format="integer">1000</item>
        <!-- 设置启动页底部品牌图，它可用来表示应用程序的生产商品牌。 -->
        <item name="android:windowSplashScreenBrandingImage" format="reference"></item>
        <!-- 设置启动页图标背景颜色，当图标颜色与窗口背景色差别不大时，设置此属性非常必要；
             注意此背景图形会与图标一样，被UI遮罩裁剪。 -->
        <item name="android:windowSplashScreenIconBackgroundColor" format="color">#000</item>
    </style>
</resources>