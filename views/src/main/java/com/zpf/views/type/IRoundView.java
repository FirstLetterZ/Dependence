package com.zpf.views.type;

public interface IRoundView {
    void setDrawCircle(boolean circle);

    boolean isDrawCircle();

    void setConnerRadius(float radius);

    float getConnerRadius();
}