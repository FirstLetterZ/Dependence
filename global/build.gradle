apply plugin: 'com.android.library'
apply from: '../publish.gradle'
android {
    namespace 'com.zpf.tool.global'
    compileSdk project.compileSdkVersion
    defaultConfig {
        minSdkVersion project.minSdkVersion
        targetSdkVersion project.targetSdkVersion
    }
}